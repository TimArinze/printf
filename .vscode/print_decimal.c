#include <main.h>

int print_decimal (int value){
    int count = 0;

    value = 98;
    if
    {
        (value >= 0);
        print_decimal(value/10);
        print_decimal(9);
        count += _putchar(value%10);
    }
    return(count)
}